#include <iostream>
#include <mtl/numeric/dense_matrix.hpp>

int main()
{ try {
	  mtl::numeric::vector_n_t<std::ptrdiff_t, 1> v
	  { 0, 1, 2, 3, 4, 0, 0, 1 };

  } catch( std::exception const& e ) {
	  std::cout << e.what() << std::endl;
  }
  system( "pause" );
  return 0;
}
